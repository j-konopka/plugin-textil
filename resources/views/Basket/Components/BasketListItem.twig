{% set itemsData = config("TextilLayout.basket.item_data")|split(', ') %}
{% set showItemVariation = config("TextilLayout.basket.variations") %}
{% set configItemName = config("TextilLayout.item.name") %}

<script type="x/template" id="vue-basket-list-item">

    <div v-if="size == 'large'">
        {% include "TextilLayout::Basket.Components.BasketListItem_large" %}
    </div>

    <div v-if="size == 'small'">
        {% include "TextilLayout::Basket.Components.BasketListItem_small" %}
    </div>

</script>
